/**
 * Represents a loading state or component.
 *
 * @returns {JSX.Element} - Returns null, indicating that this component does not render anything.
 */
export default function Loading() {
  return null
}
