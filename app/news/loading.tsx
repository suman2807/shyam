/**
 * A placeholder component that renders nothing.
 *
 * This function serves as a loading indicator or a temporary placeholder in user interfaces.
 * It is designed to be used where a visual element is needed, but the actual content is not yet available.
 *
 * @returns {null} - Always returns null, meaning it does not render any visible elements.
 */
export default function Loading() {
  return null
}
