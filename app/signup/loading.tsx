/**
 * A function that returns `null`. This is typically used as a placeholder or stub.
 *
 * @returns {null} Always returns `null`.
 *
 * Example usage:
 * ```javascript
 * const result = Loading();
 * console.log(result); // Output: null
 * ```
 */
export default function Loading() {
  return null
}
